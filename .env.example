# TuneTrivia — config-mall. Kopiera till .env och fyll i riktiga värden.
# Riktig .env committas ALDRIG (se Hårda regler i CLAUDE.md).

# Server
PORT=3000
NODE_ENV=development

# PostgreSQL
DATABASE_URL=postgres://user:password@localhost:5432/tunetrivia

# WebSocket / spel
# (lägg till spelrelaterad config här allt eftersom, t.ex. rondlängd-default)
