# docs/

Djupare referensmaterial för TuneTrivia — det som är för långt för `CLAUDE.md`
men fortfarande kanoniskt (se "Djupare referens" i `CLAUDE.md`).

Planerade dokument:

- `db-schema.md` — PostgreSQL-schema (tabeller, relationer, migrationer)
- `api.md` — HTTP-endpoints och WebSocket-events (typ + payload)
- `deploy.md` — deploy-guide när host är vald

Skapa varje fil när innehållet finns — inte tomma placeholders.
