# TuneTrivia backend — kopiera till .env och fyll i. Riktig .env committas aldrig.
PORT=3000
NODE_ENV=development
DATABASE_URL=postgres://user:password@localhost:5432/tunetrivia
# Antal frågor per omgång — total sätts server-side från detta, ej från klienten.
QUESTION_COUNT=50
